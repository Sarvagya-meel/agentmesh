# Phase 8: Contains JobDetectorAgent-specific LLM prompts and prompt templates.
# Example: prompts for filtering relevant jobs, scoring relevance.
# LLM provider must be injected via interface — not hardcoded.
