# Phase 8: Contains ApplicationAgent-specific LLM prompts and prompt templates.
# Example: cover letter generation prompt, personalisation instructions.
# LLM provider must be injected via interface — not hardcoded.
