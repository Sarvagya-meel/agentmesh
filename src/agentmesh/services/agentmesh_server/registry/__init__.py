"""Registry models and control-plane services.

Import concrete types from their defining modules so lightweight agent images can
use registry contracts without installing the registry service dependencies.
"""
