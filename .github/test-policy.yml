{
  "schema_version": 1,
  "suites": {
    "static": {"required": true},
    "unit": {"required": true},
    "integration": {"required": true},
    "docker": {"required": true},
    "uat": {"required": true},
    "smoke": {"required": true},
    "browser": {"required": true},
    "llm": {"required": false}
  },
  "waivers": [
    {
      "suite": "llm",
      "owner": "@Sarvagya-meel",
      "reason": "Live provider secrets are not configured in GitHub and LangSmith trace quota is exhausted; deterministic runtime coverage remains required.",
      "created_on": "2026-09-10",
      "expires_on": "2026-10-10"
    }
  ]
}
