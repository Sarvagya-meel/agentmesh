---
description: AgentMesh testing rules, organization, and acceptance criteria
applyTo: "**/*.py, **/*.md, **/*.yml, **/*.yaml"
---

# AgentMesh — Testing Rules

Follow `AGENTS.md` at the repository root as the single source of truth for AI
assistant instructions.

For testing context, read `.kiro/steering/testing.md`, `pyproject.toml`, and the
validation section of `README.md`.
