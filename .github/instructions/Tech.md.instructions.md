---
description: AgentMesh technology stack and dependency rules
applyTo: "**/*.py, **/*.md, **/*.yml, **/*.yaml"
---

# AgentMesh — Technology Stack

Follow `AGENTS.md` at the repository root as the single source of truth for AI
assistant instructions.

For technology and dependency context, read `.kiro/steering/tech.md`,
`pyproject.toml`, and the relevant deployment README.
