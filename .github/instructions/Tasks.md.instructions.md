---
description: AgentMesh Core implementation phases and delivery plan
applyTo: "**/*.py, **/*.md, **/*.yml, **/*.yaml"
---

# AgentMesh Core Tasks

Follow `AGENTS.md` at the repository root as the single source of truth for AI
assistant instructions.

For current implementation priorities, read `plan.md`,
`docs/agent-runtime-roadmap.md`, active issues or task prompts, and nearby package
README files. Historical phase plans are not authoritative unless the current task
explicitly references them.
