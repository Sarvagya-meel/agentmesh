---
description: AgentMesh Core product requirements and acceptance criteria
applyTo: "**/*.py, **/*.md, **/*.yml, **/*.yaml"
---

# AgentMesh Core Requirements

Follow `AGENTS.md` at the repository root as the single source of truth for AI
assistant instructions and acceptance expectations.

For runtime requirements, read `plan.md`, then
`docs/runtime/overview.md`, `docs/runtime/functional.md`,
`docs/runtime/non-functional.md`, `docs/runtime/roadmap.md`, and
`docs/runtime/api-worker-registry-guide.md`.
