# GitHub Copilot Notes

`AGENTS.md` is the single source of truth for assistant behavior. The root
`.github/copilot-instructions.md` remains as a small compatibility adapter for
GitHub Copilot.

The previous expanded instruction files were moved into
`docs/ide/github-copilot/instructions/` for reference. Keep substantive project
rules in `AGENTS.md`, `plan.md`, and the focused docs under `docs/`.
