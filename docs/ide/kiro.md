# Kiro Notes

Kiro-specific steering and hook content was migrated out of `.kiro/` so project
guidance stays IDE-neutral.

Use these canonical docs instead:

- `AGENTS.md` for assistant behavior and repository-wide guardrails.
- `plan.md` and `docs/runtime/` for runtime architecture.
- `docs/project/` for product, technology, testing, architecture, and coding
  standards.

Former hook definitions are kept under `docs/ide/kiro/hooks/` as reference
material, not active workspace automation.
