# Phase 8: Implements the main ApplicationAgent class and event handling logic.
# Subscribes to TASK_ASSIGNED (task_type=APPLY).
# Emits APPLICATION_SENT on success, TASK_FAILED on error.
# Extends BaseAgent from agents/base.py.
