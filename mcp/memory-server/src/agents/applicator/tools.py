# Phase 8: Contains ApplicationAgent-specific external integrations and helper tools.
# Example: email sending client, form submission interface, PDF generator.
# All external integrations must be behind abstract interfaces (see tech.md).
